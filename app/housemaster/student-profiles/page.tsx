'use client';
import {useEffect,useMemo,useState} from 'react';
import {useRouter} from 'next/navigation';
import {createClient} from '@/lib/supabase/client';
type P={id:string;school_id:string;full_name:string;role:string;is_active:boolean|null};type S={id:string;admission_number:string|null;full_name:string;date_of_birth:string|null;gender:string|null;guardian_name:string|null;guardian_phone:string|null;address:string|null;photo_url:string|null;resident:string|null};type Y={id:string;name:string;start_date:string;is_current:boolean|null};type E={student_id:string;academic_year_id:string;class_id:string;programme_id:string|null;status:string|null};type C={id:string;name:string;level:string|null;programme_id:string|null};type G={id:string;name:string;code:string|null};type R={student_id:string;health_insurance_number:string|null;home_location:string|null;emergency_contact_name:string|null;emergency_contact_phone:string|null;notes:string|null};type D={student_id:string;academic_year_id:string;address:string;location:string|null;landlord_caretaker_name:string|null;landlord_caretaker_phone:string|null;is_current:boolean};type H={id:string;name:string};type M={id:string;house_id:string;room_number:string;location:string|null};type A={student_id:string;academic_year_id:string;house_id:string;room_id:string;bed_space:string|null;status:string;allocated_at:string};type I={id:string;student_id:string;incident_at:string;category:string;severity:string;status:string;description:string};type O={id:string;student_id:string;checkout_type:string;checkout_at:string;destination:string|null;expected_return_at:string|null;returned_at:string|null;status:string};
const sb=createClient(),input='h-11 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 text-sm font-semibold outline-none focus:border-slate-400 focus:bg-white focus:ring-4 focus:ring-slate-100';
export default function Profiles(){const router=useRouter();const[p,setP]=useState<P|null>(null),[students,setStudents]=useState<S[]>([]),[years,setYears]=useState<Y[]>([]),[en,setEn]=useState<E[]>([]),[classes,setClasses]=useState<C[]>([]),[programmes,setProgrammes]=useState<G[]>([]),[rp,setRp]=useState<R[]>([]),[dr,setDr]=useState<D[]>([]),[houses,setHouses]=useState<H[]>([]),[rooms,setRooms]=useState<M[]>([]),[alloc,setAlloc]=useState<A[]>([]),[inc,setInc]=useState<I[]>([]),[outs,setOuts]=useState<O[]>([]),[year,setYear]=useState(''),[search,setSearch]=useState(''),[sid,setSid]=useState(''),[loading,setLoading]=useState(true),[msg,setMsg]=useState('');
async function load(school:string){const q=await Promise.all([sb.from('students').select('id,admission_number,full_name,date_of_birth,gender,guardian_name,guardian_phone,address,photo_url,resident').eq('school_id',school).eq('status','active').order('full_name'),sb.from('academic_years').select('id,name,start_date,is_current').eq('school_id',school).order('start_date',{ascending:false}),sb.from('classes').select('id,name,level,programme_id').eq('school_id',school),sb.from('programmes').select('id,name,code').eq('school_id',school),sb.from('student_residential_profiles').select('student_id,health_insurance_number,home_location,emergency_contact_name,emergency_contact_phone,notes').eq('school_id',school),sb.from('residential_houses').select('id,name').eq('school_id',school),sb.from('residential_rooms').select('id,house_id,room_number,location').eq('school_id',school),sb.from('student_incidents').select('id,student_id,incident_at,category,severity,status,description').eq('school_id',school).order('incident_at',{ascending:false}).limit(500),sb.from('student_checkouts').select('id,student_id,checkout_type,checkout_at,destination,expected_return_at,returned_at,status').eq('school_id',school).order('checkout_at',{ascending:false}).limit(500)]);const er=q.find(x=>x.error)?.error;if(er)throw new Error(er.message);setStudents((q[0].data||[])as S[]);setYears((q[1].data||[])as Y[]);setClasses((q[2].data||[])as C[]);setProgrammes((q[3].data||[])as G[]);setRp((q[4].data||[])as R[]);setHouses((q[5].data||[])as H[]);setRooms((q[6].data||[])as M[]);setInc((q[7].data||[])as I[]);setOuts((q[8].data||[])as O[]);const cy=(q[1].data||[]).find((x:any)=>x.is_current)||(q[1].data||[])[0];if(cy){setYear(cy.id);await loadYear(school,cy.id,(q[0].data||[]).map((x:any)=>x.id))}}
async function loadYear(school:string,y:string,ids=students.map(s=>s.id)){if(!ids.length){setEn([]);setDr([]);setAlloc([]);return}const[a,b,c]=await Promise.all([sb.from('enrollments').select('student_id,academic_year_id,class_id,programme_id,status').eq('academic_year_id',y).eq('status','active').in('student_id',ids),sb.from('day_student_residences').select('student_id,academic_year_id,address,location,landlord_caretaker_name,landlord_caretaker_phone,is_current').eq('school_id',school).eq('academic_year_id',y).eq('is_current',true).in('student_id',ids),sb.from('boarding_allocations').select('student_id,academic_year_id,house_id,room_id,bed_space,status,allocated_at').eq('school_id',school).eq('academic_year_id',y).eq('status','Active').in('student_id',ids)]);if(a.error||b.error||c.error)throw new Error(a.error?.message||b.error?.message||c.error?.message);setEn((a.data||[])as E[]);setDr((b.data||[])as D[]);setAlloc((c.data||[])as A[])}
useEffect(()=>{(async()=>{const{data:{user}}=await sb.auth.getUser();if(!user){router.replace('/login');return}const{data:u}=await sb.from('users').select('id,school_id,full_name,role,is_active').eq('id',user.id).maybeSingle();if(!u||u.is_active===false||!['housemaster','admin'].includes(u.role)){router.replace('/login');return}setP(u as P);try{await load(u.school_id)}catch(e){setMsg(e instanceof Error?e.message:'Unable to load profiles.')}finally{setLoading(false)}})()},[router]);
async function changeYear(y:string){if(!p)return;setYear(y);setLoading(true);try{await loadYear(p.school_id,y)}catch(e){setMsg(e instanceof Error?e.message:'Unable to load year.')}finally{setLoading(false)}}
const cm=useMemo(()=>new Map(classes.map(x=>[x.id,x])),[classes]),gm=useMemo(()=>new Map(programmes.map(x=>[x.id,x])),[programmes]),hm=useMemo(()=>new Map(houses.map(x=>[x.id,x])),[houses]),mm=useMemo(()=>new Map(rooms.map(x=>[x.id,x])),[rooms]),em=useMemo(()=>new Map(en.map(x=>[x.student_id,x])),[en]),rpm=useMemo(()=>new Map(rp.map(x=>[x.student_id,x])),[rp]),dm=useMemo(()=>new Map(dr.map(x=>[x.student_id,x])),[dr]),am=useMemo(()=>new Map(alloc.map(x=>[x.student_id,x])),[alloc]);
const list=students.filter(s=>`${s.full_name} ${s.admission_number||''} ${s.resident||''}`.toLowerCase().includes(search.toLowerCase())).slice(0,100),s=students.find(x=>x.id===sid),e=s?em.get(s.id):undefined,c=e?cm.get(e.class_id):undefined,g=e?gm.get(e.programme_id||c?.programme_id||''):undefined,r=s?rpm.get(s.id):undefined,d=s?dm.get(s.id):undefined,a=s?am.get(s.id):undefined,h=a?hm.get(a.house_id):undefined,room=a?mm.get(a.room_id):undefined,si=s?inc.filter(x=>x.student_id===s.id):[],so=s?outs.filter(x=>x.student_id===s.id):[],away=so.find(x=>x.status==='Checked Out');
if(loading&&!p)return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto h-40 max-w-7xl animate-pulse rounded-3xl bg-slate-900"/></main>;
return <main className="min-h-screen bg-slate-50 px-4 py-5 sm:px-6 lg:px-8"><link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"/><div className="mx-auto max-w-7xl space-y-6"><header className="rounded-3xl bg-gradient-to-br from-slate-950 via-slate-900 to-slate-800 p-6 text-white shadow-xl sm:p-8"><span className="rounded-full bg-white/10 px-3 py-1.5 text-xs font-bold"><i className="fa-solid fa-id-card mr-2"/>360° Residential Profile</span><h1 className="mt-4 text-2xl font-black sm:text-3xl">Student Profiles</h1><p className="mt-2 text-sm text-slate-300">One residential view of identity, academics, contacts, accommodation, incidents and movement history.</p></header>{msg&&<div className="rounded-2xl border border-red-200 bg-red-50 p-3 text-sm font-semibold text-red-700">{msg}</div>}
<section className="rounded-3xl border border-slate-200 bg-white p-4 shadow-sm"><div className="grid gap-3 md:grid-cols-[220px_1fr]"><select className={input} value={year} onChange={x=>changeYear(x.target.value)}>{years.map(y=><option key={y.id} value={y.id}>{y.name}</option>)}</select><div className="relative"><i className="fa-solid fa-magnifying-glass absolute left-4 top-1/2 -translate-y-1/2 text-slate-400"/><input className={`${input} pl-11`} value={search} onChange={x=>setSearch(x.target.value)} placeholder="Search student or admission number..."/></div></div><div className="mt-3 flex max-h-48 flex-wrap gap-2 overflow-auto">{list.map(x=><button key={x.id} onClick={()=>setSid(x.id)} className={`rounded-xl border px-3 py-2 text-xs font-bold ${sid===x.id?'border-slate-900 bg-slate-900 text-white':'border-slate-200 bg-slate-50 text-slate-700'}`}>{x.full_name} · {x.admission_number||'No ID'}</button>)}</div></section>
{s?<><section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><div className="flex h-24 w-24 shrink-0 items-center justify-center overflow-hidden rounded-3xl bg-slate-100 text-2xl font-black text-slate-500">{s.photo_url?<img src={s.photo_url} alt="" className="h-full w-full object-cover"/>:s.full_name.split(' ').map(x=>x[0]).slice(0,2).join('')}</div><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><h2 className="text-2xl font-black text-slate-900">{s.full_name}</h2><B text={s.resident==='Boarding'?'Boarder':s.resident||'Unassigned'}/><B text={away?'Away':'On Campus'}/></div><p className="mt-2 text-sm text-slate-500">{s.admission_number||'No admission number'} · {g?.code||g?.name||'No programme'} · {c?.level||'No form'} · {c?.name||'No class'}</p></div></div></section>
<div className="grid gap-5 lg:grid-cols-2"><Card icon="fa-user" title="Personal & Guardian"><Row l="Gender" v={s.gender}/><Row l="Date of Birth" v={s.date_of_birth}/><Row l="Guardian" v={s.guardian_name}/><Row l="Guardian Phone" v={s.guardian_phone}/><Row l="Home Address" v={s.address}/></Card><Card icon="fa-house" title="Residential Information">{s.resident==='Boarding'?<><Row l="House" v={h?.name}/><Row l="Room" v={room?.room_number}/><Row l="Room Location" v={room?.location}/><Row l="Bed Space" v={a?.bed_space}/><Row l="Health Insurance" v={r?.health_insurance_number}/><Row l="Home Location" v={r?.home_location}/><Row l="Emergency Contact" v={r?.emergency_contact_name}/><Row l="Emergency Phone" v={r?.emergency_contact_phone}/></>:<><Row l="Residential Address" v={d?.address}/><Row l="Location" v={d?.location}/><Row l="Landlord / Caretaker" v={d?.landlord_caretaker_name}/><Row l="Caretaker Phone" v={d?.landlord_caretaker_phone}/></>}</Card></div>
<div className="grid gap-5 lg:grid-cols-2"><Card icon="fa-triangle-exclamation" title={`Incident History (${si.length})`}>{si.length?si.slice(0,10).map(x=><div key={x.id} className="border-b border-slate-100 py-3 last:border-0"><div className="flex flex-wrap gap-2"><b className="text-sm text-slate-800">{x.category}</b><B text={x.severity}/><B text={x.status}/></div><p className="mt-1 text-xs text-slate-500">{new Date(x.incident_at).toLocaleString('en-GB')}</p><p className="mt-2 text-xs leading-5 text-slate-600">{x.description}</p></div>):<Empty text="No incident history recorded."/ >}</Card><Card icon="fa-clock-rotate-left" title={`Movement History (${so.length})`}>{so.length?so.slice(0,10).map(x=><div key={x.id} className="border-b border-slate-100 py-3 last:border-0"><div className="flex flex-wrap gap-2"><b className="text-sm text-slate-800">{x.checkout_type}</b><B text={x.status}/></div><p className="mt-1 text-xs text-slate-500">Out: {new Date(x.checkout_at).toLocaleString('en-GB')}{x.returned_at?` · Returned: ${new Date(x.returned_at).toLocaleString('en-GB')}`:''}</p><p className="mt-1 text-xs text-slate-600">{x.destination||'No destination recorded'}</p></div>):<Empty text="No checkout / vacation history recorded."/ >}</Card></div></>:<div className="rounded-3xl border border-dashed border-slate-300 bg-white p-12 text-center"><i className="fa-solid fa-address-card text-3xl text-slate-300"/><h2 className="mt-4 font-black text-slate-900">Select a Student</h2><p className="mt-2 text-sm text-slate-500">Search and select a student to open their complete residential profile.</p></div>}</div></main>}
function Card({icon,title,children}:{icon:string;title:string;children:React.ReactNode}){return <section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-black text-slate-900"><i className={`fa-solid ${icon} mr-2`}/>{title}</h3><div className="mt-4">{children}</div></section>}function Row({l,v}:{l:string;v:string|null|undefined}){return <div className="flex justify-between gap-4 border-b border-slate-100 py-2.5 text-sm last:border-0"><span className="text-slate-500">{l}</span><b className="text-right text-slate-800">{v||'—'}</b></div>}function B({text}:{text:string}){return <span className="rounded-full bg-slate-100 px-2 py-1 text-[10px] font-bold text-slate-700">{text}</span>}function Empty({text}:{text:string}){return <div className="py-8 text-center text-xs font-semibold text-slate-500">{text}</div>}
